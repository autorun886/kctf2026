## ==设计哲学==

核心原则：**真实但不可组合** — 每个组件都是可理解的，但正确答案只存在于所有组件在正确状态下的交汇点。

**Flag 的角色**：flag 不是"被加密的答案"，而是"修复参数"。程序被故意破坏，选手输入 flag 后程序自我修复到唯一正确状态。

**AI 对抗原则**：程序的命名、常量、结构全部伪装为已知算法的实现。AI 工具会基于模式匹配给出"自信但错误"的分析，将使用者引向蜜罐路径。人类逆向者通过实际执行和数据流追踪才能发现真相。

**两种实现方案**：
- 方案 A（显式修复）：flag 直接驱动代码/数据修复，修复过程可被识别。低难度版本。
- 方案 B（隐式修复）：修复伪装为参数化密码系统，逆向者无法区分"修复"与"加密"。高难度版本。

---

## ==方案 A：显式修复==

> flag 字节被拆分后写入代码段/数据段，修复过程直接可见。
> 选手能识别"输入在修复程序"的模式，核心挑战在于逆向各修复点的约束和它们之间的非线性依赖。

### 破坏层设计

程序发布时处于"不完整"状态，四类破坏同时存在：

#### 1. 控制流破坏（CFR）

程序核心计算由 8 个 basic block (BB0~BB7) 组成，编译时各 BB 入口地址固定：

**BB0→BB1 跳转目标加密**：
- ARM64 的 B 指令格式：`[31:26]=000101 [25:0]=imm26`
- 发布时 imm26 字段被 XOR：`insn.imm26 ^= *(uint32_t*)&flag[0] & 0x03FFFFFF`
- 修复时用相同 key 再次 XOR 还原

**BB2→BB3 条件码反转**：
- ARM64 CSEL 指令格式：`[15:12]=cond`
- 发布时 cond 字段被 flag[4] 的低 4 bit XOR
- 正确的 cond 值由 BB2 的比较指令决定（唯一）

**BB4→BB5 dead block 插入**：
- BB4 末尾的 B 指令目标指向 dead block 而非 BB5
- dead block 包含合法但无意义的 ARM64 指令（不会崩溃，但计算结果错误）
- 修复：将 B 指令的 imm26 改为跳过 dead block 直达 BB5

**BB6→BB7 dispatch table 加密**：
- 间接跳转 `br xN`，xN 从 dispatch_table[idx] 加载
- table 中 4 个条目的偏移值被 `flag[5:9] ^ soKey[0:4]` XOR 加密
- 修复后 table 条目指向 BB7 的正确入口

#### 1.1 跳转修复后范围限定（新增）

修复跳转指令后，立即进行合法性校验。防止随机 flag 值产生"恰好不崩溃但计算错误"的跳转目标：

```c
// 合法 BB 入口地址表（编译时确定，存储为相对 .text 基址的偏移）
static const uint32_t VALID_BB_OFFSETS[] = {
    BB0_OFF, BB1_OFF, BB2_OFF, BB3_OFF,
    BB4_OFF, BB5_OFF, BB6_OFF, BB7_OFF
};

// 修复后校验：跳转目标必须落在合法 BB 入口 ±4 字节内
int validate_branch_target(uint32_t repaired_insn, uintptr_t insn_addr) {
    // 从 B 指令提取 imm26，符号扩展，×4 得到偏移
    int32_t offset = ((int32_t)(repaired_insn << 6)) >> 6;
    offset <<= 2;
    uintptr_t target = insn_addr + offset;
    uint32_t target_off = (uint32_t)(target - text_base);

    // 必须精确命中某个 BB 入口（4 字节对齐）
    for (int i = 0; i < 8; i++) {
        if (target_off == VALID_BB_OFFSETS[i])
            return 1;  // 合法
    }
    return 0;  // 非法：flag 错误
}

// dispatch table 修复后校验
int validate_dispatch_table(uint32_t *table) {
    for (int i = 0; i < 4; i++) {
        uint32_t off = table[i];
        // 必须指向 BB7 内部的合法入口点（BB7 有 4 个 case）
        if (off < BB7_OFF || off >= BB7_OFF + BB7_SIZE)
            return 0;
        // 必须 4 字节对齐
        if (off & 0x3)
            return 0;
    }
    return 1;
}
```

**范围限定的安全意义**：
- 26-bit imm26 有 2^26 ≈ 67M 种可能值，但合法目标仅 8 个 → 随机命中概率 < 10^-7
- dispatch table 4 个条目各自约束在 BB7 范围内 → 联合概率极低
- 校验失败时不给出具体错误信息（统一返回 "Invalid"），不泄露哪个修复点出错
- **蜜罐联动**：校验失败时跳转到蜜罐计算路径（见 AI 蜜罐章节），产出看似合理但错误的结果

修复实现流程：
```c
void repair_cfg(const uint8_t *flag, const uint8_t *so_key) {
    // 1. mprotect 使代码页可写
    void *page = (void *)((uintptr_t)text_start & ~0xFFF);
    size_t len = text_end - page + 0xFFF & ~0xFFF;
    mprotect(page, len, PROT_READ | PROT_WRITE | PROT_EXEC);
    
    // 2. 修复 BB0→BB1
    uint32_t *b_insn = (uint32_t *)BB0_BRANCH_ADDR;
    *b_insn ^= (*(uint32_t *)&flag[0]) & 0x03FFFFFF;
    
    // 3. 范围校验（新增）
    if (!validate_branch_target(*b_insn, (uintptr_t)b_insn)) {
        // 内联蜜罐：不调用共享函数，直接篡改 S-Box 为恒等映射
        for (int i = 0; i < 256; i++) sbox_shipped[i] = i;
        return;
    }
    
    // 4. 修复 BB2→BB3 条件码
    uint32_t *csel_insn = (uint32_t *)BB2_CSEL_ADDR;
    *csel_insn ^= ((uint32_t)(flag[4] & 0x0F)) << 12;
    
    // 5. 修复 dispatch table（数据段，不需要额外 mprotect）
    uint32_t *table = (uint32_t *)DISPATCH_TABLE_ADDR;
    uint32_t table_key = *(uint32_t *)&flag[5] ^ *(uint32_t *)&so_key[0];
    for (int i = 0; i < 4; i++)
        table[i] ^= table_key;
    
    // 6. dispatch table 范围校验（新增）
    if (!validate_dispatch_table(table)) {
        // 内联蜜罐：篡改 delta 为 TEA 的黄金比例常量（差 1）
        xtea_delta = 0x9E3779B8;
        return;
    }
    
    // 7. 恢复代码页权限 + 刷新 icache
    mprotect(page, len, PROT_READ | PROT_EXEC);
    __builtin___clear_cache((char *)text_start, (char *)text_end);
}
```

唯一性保证：
- BB1 入口地址固定 → imm26 只有一个正确值 → flag[0:4] 唯一
- CSEL 的正确 cond 由前置 CMP 决定 → flag[4] 低 4 bit 唯一
- dispatch table 的正确偏移 = BB7 入口 - table 基址 → flag[5:9] ^ soKey[0:4] 唯一

#### 2. 数据破坏（S-Box 修复）

```c
// 发布时的 S-Box（非双射，被 XOR 破坏）
static uint8_t sbox_shipped[256];

void repair_sbox(const uint8_t *flag, uint8_t cfg_dependency) {
    uint32_t seed = *(uint32_t *)&flag[9];
    
    // xorshift32 PRNG 展开 256 字节
    uint32_t xs = seed;
    uint8_t key_stream[256];
    for (int i = 0; i < 256; i++) {
        xs ^= xs << 13;
        xs ^= xs >> 17;
        xs ^= xs << 5;
        key_stream[i] = (uint8_t)xs;
    }
    
    // XOR 还原，起始偏移由 CFG 修复结果决定（非线性耦合）
    // cfg_dependency = dispatch_table[0] 修复后的低 8 bit
    uint8_t offset = cfg_dependency;
    for (int i = 0; i < 256; i++)
        sbox_shipped[(i + offset) & 0xFF] ^= key_stream[i];
}
```

唯一性保证：
- 正确的 S-Box 必须是双射（256 个不同值各出现恰好一次）
- xorshift32 周期 2^32-1，不同 seed 产出不同 key_stream
- 给定目标置换和 XOR 结构，seed 唯一（可通过逆 Fisher-Yates 数学推导）

#### 3. 常量破坏（XTEA 参数修复）

```c
static uint32_t xtea_delta;
static uint32_t round_constants[32];

void repair_constants(const uint8_t *flag, uint8_t sbox_first) {
    // delta 直接恢复
    xtea_delta = *(uint32_t *)&flag[13];
    
    // LCG seed 混入修复后 S-Box 的首字节（非线性耦合）
    uint32_t lcg = *(uint32_t *)&flag[17];
    lcg ^= (uint32_t)sbox_first * 0x01010101u;
    
    // Numerical Recipes LCG 展开 32 个轮常量
    for (int i = 0; i < 32; i++) {
        lcg = lcg * 1664525u + 1013904223u;
        round_constants[i] = lcg;
    }
}
```

唯一性保证：
- 内嵌 3 组已知 (plaintext, ciphertext) 对，每组 64 bit
- 魔改 XTEA 加密：`encrypt(pt, delta, round_constants) == ct`
- 3 组提供 192 bit 约束，自由度仅 64 bit（delta 32 + seed 32）→ 过约束，唯一解

#### 4. 语义破坏（函数补全）

```c
// step2：循环左移。发布时被替换为 NOP (mov x0, x0)
static inline uint32_t step2(uint32_t val, uint8_t amount) {
    amount &= 0x1F;  // 0~31
    return (val << amount) | (val >> (32 - amount));
}

// step3：非线性混合。发布时参数为零导致退化为恒等
static inline uint32_t step3(uint32_t state, uint32_t param) {
    // param 有效位数由 round_constants[0] 高 4 bit 决定（非线性耦合）
    uint8_t bits = 16 + ((round_constants[0] >> 28) & 0x0F);  // 16~31 bit
    uint32_t mask = (1u << bits) - 1;
    param &= mask;
    return state ^ ((state >> 5) + param) ^ ((state << 4) + (param >> 12));
}
```

唯一性保证：
- 内嵌 8 组 (input, expected_output) 测试对
- amount 范围 0~31（5 bit），param 范围取决于 bits（16~31 bit）
- 8 组测试对提供 256 bit 约束，远超自由度 → 唯一解

### 修复点间的非线性耦合（详细）

```
repair_cfg(flag[0:9], soKey)
    ↓ 产出：dispatch_table[0] 修复后的值
repair_sbox(flag[9:13], dispatch_table[0] & 0xFF)
    ↓ 产出：sbox[0] 修复后的值
repair_constants(flag[13:21], sbox[0])
    ↓ 产出：round_constants[0]
repair_semantics(flag[21:25], round_constants[0] >> 28)
```

每一步的输出作为下一步的输入参数。选手必须按序求解：
1. 先从合法 BB 地址反推 flag[0:9]（纯结构约束，不依赖其他修复点）
2. 用 dispatch_table[0] 确定 S-Box XOR 起始偏移，再逆推 flag[9:13]
3. 用 sbox[0] 确定 LCG 混入值，再用明密文对约束 flag[13:21]
4. 用 round_constants[0] 确定 step3 有效位数，再用 IO 对约束 flag[21:25]

### Flag 结构（方案 A）

```
字节 [0:4]   → BB0 branch imm26 XOR key
字节 [4:5]   → BB2 csel cond XOR（低 4 bit 有效）
字节 [5:9]   → dispatch table XOR key（与 soKey[0:4] 联合）
字节 [9:13]  → S-Box xorshift32 seed
字节 [13:17] → XTEA delta（直接值）
字节 [17:21] → 轮常量 LCG seed（混入 sbox[0]）
字节 [21:22] → step2 循环移位量（低 5 bit 有效）
字节 [22:25] → step3 参数（有效位数由 round_constants[0] 决定）
总计 25 字节有效载荷，base64 编码后作为用户输入
```

### 执行与校验（方案 A）

```
1. 用户输入 flag（base64 → 25 字节）
2. JNI 调用 nativeProcessInput(flag_bytes)
3. Native 内部 JNI 回调 Java deriveNativeKey() → soKey[16]
4. repair_cfg(flag, soKey) — 含范围校验，失败走蜜罐
5. repair_sbox(flag, dispatch_table[0] & 0xFF)
6. repair_constants(flag, sbox[0])
7. repair_semantics(flag, round_constants[0] >> 28)
8. mprotect(.text, RX) + icache flush
9. 执行 16 轮计算（初始 state = 硬编码 16 字节 IV）
10. 解密 ENC_EXPECTED_STATE（用 soKey XOR）→ expected_state
11. memcmp(final_state, expected_state, 16) == 0 → "Correct"
```

---

## ==方案 B：隐式修复==

> 程序看起来是一个正常的参数化密码系统。
> 没有 mprotect、没有代码段写入、没有显式 repair 函数。纯数据驱动。
> 选手无法区分"flag 在驱动修复"还是"flag 在参与加密"。

### 架构

```
flag(25B) → key_schedule(flag, soKey[16B]) → runtime_params → SPN(16轮, state=16B) → 比对
state 初始值：硬编码 16 字节 IV
验证：final_state == decrypt(ENC_EXPECTED_STATE, soKey)
```

### Key Schedule

expand_key_material 是核心的不可逆扩展函数。选手需要逆向此函数才能从已知 params 反推 flag。

```c
struct round_config {
    uint8_t sbox_selector;   // 2 bit: 4 个 S-Box 选一
    uint8_t shift_pattern;   // 2 bit: 4 种 ShiftRows 选一
    uint8_t mix_matrix_idx;  // 2 bit: 4 个 MDS 矩阵选一
    uint8_t nonlinear_mode;  // 2 bit: 4 种非线性模式选一
};

struct runtime_params {
    uint32_t round_keys[16];
    struct round_config configs[16];
    uint32_t sbox_seeds[4];
    uint32_t delta;
};

// 魔改密钥扩展：基于 Speck-like ARX 结构，非标准
void expand_key_material(const uint8_t *input, uint8_t *out, int out_len) {
    uint64_t s[4];
    memcpy(s, input, 25);  // 25 字节 flag 填充到 32 字节状态
    memset((uint8_t*)s + 25, 0x5A, 7);  // padding

    // 12 轮 ARX 混合
    for (int r = 0; r < 12; r++) {
        s[0] = (ror64(s[0], 8) + s[1]) ^ r;
        s[1] = rol64(s[1], 3) ^ s[0];
        s[2] = (ror64(s[2], 8) + s[3]) ^ (r + 4);
        s[3] = rol64(s[3], 3) ^ s[2];
        // 跨半区混合
        s[0] ^= s[3];
        s[2] ^= s[1];
    }

    // 从状态中挤出 out_len 字节（sponge-like）
    int pos = 0;
    while (pos < out_len) {
        int chunk = (out_len - pos) < 32 ? (out_len - pos) : 32;
        memcpy(out + pos, s, chunk);
        pos += chunk;
        // 额外置换
        s[0] += s[2]; s[1] ^= s[3];
        s[2] = rol64(s[2], 17); s[3] = ror64(s[3], 11);
    }
}

void key_schedule(const uint8_t *flag, const uint8_t *so_key,
                  struct runtime_params *params) {
    // material 布局：
    //   [0:96]   ← expand_key_material 输出
    //   [96:112] ← material[0:16] ^ soKey[0:16]（注意：delta 从此处派生，同时依赖 flag 和 soKey）
    //   [112:128]← material[32:48] ^ ipc[0:16]
    uint8_t material[128];
    expand_key_material(flag, material, 96);

    // soKey 混入（XOR 到 material[96:112]）
    for (int i = 0; i < 16; i++)
        material[96 + i] = material[i] ^ so_key[i];

    // 子进程 secret 混入（可选层）
    uint8_t ipc[16];
    get_ipc_material(ipc);  // ptrace 失败时返回 fallback
    for (int i = 0; i < 16; i++)
        material[112 + i] = material[32 + i] ^ ipc[i];

    // 派生 round_keys：每 4 字节一个
    for (int i = 0; i < 16; i++)
        params->round_keys[i] = *(uint32_t *)(material + i * 4);

    // 派生 configs：material[64:80] 的每字节拆为 4 个 2-bit 字段
    for (int i = 0; i < 16; i++) {
        uint8_t b = material[64 + i];
        params->configs[i].sbox_selector  = (b >> 0) & 0x03;
        params->configs[i].shift_pattern  = (b >> 2) & 0x03;
        params->configs[i].mix_matrix_idx = (b >> 4) & 0x03;
        params->configs[i].nonlinear_mode = (b >> 6) & 0x03;
    }

    // 派生 sbox_seeds
    for (int i = 0; i < 4; i++)
        params->sbox_seeds[i] = *(uint32_t *)(material + 80 + i * 4);

    // 派生 delta
    params->delta = *(uint32_t *)(material + 96);
}
```

### S-Box 生成（Fisher-Yates shuffle）

```c
void generate_sbox(uint32_t seed, uint8_t sbox[256]) {
    // 初始化为恒等置换
    for (int i = 0; i < 256; i++)
        sbox[i] = i;

    // xorshift32 驱动的 Fisher-Yates shuffle
    uint32_t xs = seed;
    for (int i = 255; i > 0; i--) {
        xs ^= xs << 13; xs ^= xs >> 17; xs ^= xs << 5;
        uint8_t j = xs % (i + 1);
        uint8_t tmp = sbox[i]; sbox[i] = sbox[j]; sbox[j] = tmp;
    }
    // 结果必然是双射（Fisher-Yates 保证）
}
```

4 个不同的 seed 产出 4 个不同的置换。验证时需确认 4 个 S-Box 无仿射等价关系（DDT 分布不同）。

### MDS 矩阵（GF(2^8) 上的运算）

GF(2^8) 使用不可约多项式 `x^8 + x^4 + x^3 + x + 1`（即 0x11B，与 AES 相同）。

4 个 4×4 MDS 矩阵，每个的最小距离 = 5（任意 4×4 子矩阵满秩）：

```c
// 矩阵元素是 GF(2^8) 上的值
static const uint8_t MDS[4][4][4] = {
    // M0: 基于 Cauchy 矩阵构造
    {{0x02, 0x03, 0x01, 0x01},
     {0x01, 0x02, 0x03, 0x01},
     {0x01, 0x01, 0x02, 0x03},
     {0x03, 0x01, 0x01, 0x02}},
    // M1
    {{0x05, 0x03, 0x04, 0x02},
     {0x02, 0x05, 0x03, 0x04},
     {0x04, 0x02, 0x05, 0x03},
     {0x03, 0x04, 0x02, 0x05}},
    // M2
    {{0x07, 0x06, 0x02, 0x03},
     {0x03, 0x07, 0x06, 0x02},
     {0x02, 0x03, 0x07, 0x06},
     {0x06, 0x02, 0x03, 0x07}},
    // M3
    {{0x09, 0x0E, 0x05, 0x04},
     {0x04, 0x09, 0x0E, 0x05},
     {0x05, 0x04, 0x09, 0x0E},
     {0x0E, 0x05, 0x04, 0x09}}
};

// GF(2^8) 乘法
uint8_t gf_mul(uint8_t a, uint8_t b) {
    uint8_t r = 0;
    for (int i = 0; i < 8; i++) {
        if (b & 1) r ^= a;
        uint8_t hi = a & 0x80;
        a <<= 1;
        if (hi) a ^= 0x1B;
        b >>= 1;
    }
    return r;
}

// MixColumns
void mix_columns_mds(uint8_t *state, const uint8_t matrix[4][4]) {
    for (int col = 0; col < 4; col++) {
        uint8_t in[4], out[4];
        for (int i = 0; i < 4; i++) in[i] = state[col * 4 + i];
        for (int i = 0; i < 4; i++) {
            out[i] = 0;
            for (int j = 0; j < 4; j++)
                out[i] ^= gf_mul(matrix[i][j], in[j]);
        }
        for (int i = 0; i < 4; i++) state[col * 4 + i] = out[i];
    }
}
```

### ShiftRows 模式

```c
static const uint8_t SHIFTS[4][4] = {
    {0, 1, 2, 3},   // 模式 0：标准 AES 移位
    {0, 1, 3, 4},   // 模式 1
    {0, 2, 3, 1},   // 模式 2
    {0, 3, 1, 2}    // 模式 3
};

void shift_rows(uint8_t *state, const uint8_t shifts[4]) {
    uint8_t tmp[16];
    memcpy(tmp, state, 16);
    for (int row = 0; row < 4; row++) {
        uint8_t s = shifts[row] & 0x03;
        for (int col = 0; col < 4; col++)
            state[row + 4 * col] = tmp[row + 4 * ((col + s) % 4)];
    }
}
```

### 非线性反馈层

```c
static const uint8_t NL_POWER[4] = {3, 5, 7, 11};

uint8_t gf_pow(uint8_t base, uint8_t exp) {
    uint8_t result = 1;
    while (exp > 0) {
        if (exp & 1) result = gf_mul(result, base);
        base = gf_mul(base, base);
        exp >>= 1;
    }
    return result;
}

void nonlinear_feedback(uint8_t *state, uint8_t mode,
                        uint32_t delta, int round) {
    uint8_t power = NL_POWER[mode & 0x03];
    uint8_t round_const = (uint8_t)((delta >> (round % 4 * 8)) & 0xFF);

    for (int i = 0; i < 16; i++) {
        uint8_t x = state[i] ^ round_const ^ (uint8_t)round;
        state[i] = gf_pow(x, power);
    }
}
```

### 唯一性保证（结构性证明）

**定理**：对于固定的 round_keys 和 sbox_seeds，不同的 configs[16] 必然产出不同的 final_state。

**证明**：设 C ≠ C'，第一个不同的轮为 round k。
- sbox_selector 不同 → 不同 S-Box 是不同置换，对 16 字节输入至少 1 字节输出不同 → MDS 扩散到全部 4 字节
- shift_pattern 不同 → SubBytes 输出被送入 MixColumns 的不同列组合 → 输出不同
- mix_matrix_idx 不同 → 两个 MDS 矩阵之差满秩 → 对非零输入产出不同
- nonlinear_mode 不同 → 不同幂次多项式 → 对非零输入产出不同

round k 输出不同 → round k+1 输入不同 → AddRoundKey 注入不同 round_key 阻止对称性 → 雪崩传播到 final_state。∎

**expand_key_material 单射性**：25 字节输入填充到 32 字节（256-bit）状态后，12 轮 ARX 是状态空间上的双射（每步操作可逆）。输入空间（200 bit）< 状态空间（256 bit），双射限制在子集上仍为单射。squeeze 阶段的置换也是双射，因此不同 post-ARX 状态产出不同 material。结论：flag → material 是单射的，不同 flag 必然产出不同 params，结合上述定理，必然产出不同 final_state。

**最终验证的唯一性**：`memcmp(final_state, expected_state, 16)` 直接比对 16 字节状态。由于 flag → final_state 是单射的，只有唯一的 flag 能产出匹配的 final_state。

---

## ==高耦合机制（防分段攻击）==

### 设计目标

当前架构是单向线性链：`flag → params → SPN → state`。逆向者可以分段攻击：独立分析 SPN 结构，再独立逆推 key_schedule，最后拼接。高耦合机制在后 4 轮引入反馈，迫使选手必须正向模拟到第 12 轮才能确定后续行为，同时保持前 12 轮可静态分析（Z3 友好）。

### 机制 1：轮密钥动态反馈（state → round_key，仅后 4 轮）

key_schedule 预计算的 round_keys[16] 在前 12 轮直接使用，后 4 轮与当前 state 混合：

```c
void spn_encrypt(uint8_t *state, const struct runtime_params *params,
                 uint8_t sboxes[4][256]) {
    for (int round = 0; round < 16; round++) {
        // 动态轮密钥：后 4 轮 XOR 当前 state 前 4 字节
        uint32_t dynamic_key = params->round_keys[round];
        if (round >= 12) {
            dynamic_key ^= *(uint32_t *)&state[0];  // 后 4 轮反馈
        }

        spn_round(state, &params->configs[round],
                  dynamic_key, params->delta, round, sboxes);
    }
}
```

**效果**：
- 前 12 轮可独立静态分析（选手有明确立足点，Z3 可分段求解）
- 后 4 轮必须正向模拟（实际密钥依赖当前 state，阻止完全静态分析）
- 与"后 4 轮 S-Box 动态选择"对齐，概念统一：round >= 12 = 动态区

**选手仍可分析的部分**：
- key_schedule 的 ARX 结构可以独立逆向（输入输出关系不变）
- 前 12 轮的密钥 = round_keys[0:12]（无反馈），可完全静态确定
- 后 4 轮约束规模有限（4 轮 × 16 S-Box = 64 次查表），Z3 可控

### 机制 2：S-Box 后 4 轮动态选择（state → config）

前 12 轮 S-Box 选择由 config 静态决定，后 4 轮加入 state 依赖：

```c
void spn_round(uint8_t *state, const struct round_config *cfg,
               uint32_t round_key, uint32_t delta, int round,
               uint8_t sboxes[4][256]) {
    // S-Box 选择：前 12 轮静态，后 4 轮动态
    uint8_t sbox_sel;
    if (round < 12) {
        sbox_sel = cfg->sbox_selector;
    } else {
        // 后 4 轮：config 值 XOR state[0] 的低 2 bit
        sbox_sel = (cfg->sbox_selector ^ state[0]) & 0x03;
    }

    apply_sbox(state, sbox_sel, sboxes);
    shift_rows(state, SHIFTS[cfg->shift_pattern]);
    mix_columns_mds(state, MDS[cfg->mix_matrix_idx]);
    nonlinear_feedback(state, cfg->nonlinear_mode, delta, round);
    add_round_key_full(state, round_key);
}
```

**效果**：
- 前 12 轮可静态分析（选手有立足点）
- 后 4 轮的 S-Box 选择取决于第 11 轮输出 → 必须模拟到第 11 轮才能确定后续行为
- 4^4 = 256 种可能的后 4 轮 S-Box 组合，但只有 1 种是正确的

### 机制 3：soKey 双向验证（无分支污染）

soKey 不仅单向注入 key_schedule，还通过 round_keys[15] 反向验证一致性：

```c
void key_schedule(const uint8_t *flag, const uint8_t *so_key,
                  struct runtime_params *params) {
    // ... 正常的 key_schedule 流程 ...
    // 派生完 round_keys, configs, sbox_seeds, delta 后：

    // soKey 双向验证（无分支）
    uint32_t check = params->round_keys[15] ^ *(uint32_t *)&so_key[12];
    // EXPECTED_SOKEY_CHECK 是编译时用正确 flag + 正确 soKey 预计算的值
    uint32_t diff = check ^ EXPECTED_SOKEY_CHECK;
    // diff == 0 时不影响 delta；diff != 0 时污染 delta
    // 用算术避免分支：(diff | -diff) >> 31 = 0 当 diff==0，= 1 当 diff!=0
    uint32_t poison = ((diff | (~diff + 1)) >> 31) * 0xDEADBEEF;
    params->delta ^= poison;
}
```

**效果**：
- 正确 soKey → check 匹配 → poison = 0 → delta 不变
- 错误 soKey（patch/hook）→ check 不匹配 → delta 被 0xDEADBEEF 污染 → 全链路错误
- 无 if 分支，IDA 看不到条件跳转，AI 分析不出"这里有校验"
- 选手从 .so 正确派生 soKey 则完全不受影响

### 机制 4：蜜罐无分支折叠（蜜罐 B 和 D）

蜜罐 B（时间检测）和蜜罐 D（断点检测）改为无分支实现：

```c
// ===== 蜜罐 B：无分支版本 =====
void expand_key_material(const uint8_t *input, uint8_t *out, int out_len) {
    adapt_cache_strategy();  // 写入 g_cache_policy

    uint64_t s[4];
    memcpy(s, input, 25);
    memset((uint8_t*)s + 25, 0x5A, 7);

    // 无分支：正常 policy=3 → penalty=0 → rounds=12
    //         异常 policy=7 → penalty=4 → rounds=8
    int penalty = (g_cache_policy != 0x03) * 4;  // 编译为 CSEL，无跳转
    int rounds = 12 - penalty;

    for (int r = 0; r < rounds; r++) {
        s[0] = (ror64(s[0], 8) + s[1]) ^ r;
        s[1] = rol64(s[1], 3) ^ s[0];
        s[2] = (ror64(s[2], 8) + s[3]) ^ (r + 4);
        s[3] = rol64(s[3], 3) ^ s[2];
        s[0] ^= s[3];
        s[2] ^= s[1];
    }
    // ... squeeze phase ...
}

// ===== 蜜罐 D：无分支版本 =====
void nonlinear_feedback(uint8_t *state, uint8_t mode,
                        uint32_t delta, int round) {
    if (round == 0) calibrate_frame_budget();

    uint8_t power = NL_POWER[mode & 0x03];
    uint8_t round_const = (uint8_t)((delta >> (round % 4 * 8)) & 0xFF);

    // 无分支：budget_flag = 0(正常) 或 1(异常)
    uint8_t budget_flag = (g_frame_budget_ns < 16000000ULL);

    for (int i = 0; i < 16; i++) {
        uint8_t x = state[i] ^ round_const ^ (uint8_t)round;
        uint8_t correct = gf_pow(x, power);
        // 正常时 poison=0，结果=correct
        // 异常时 poison=correct^(简单XOR结果)，结果被污染
        uint8_t simple = x ^ power;  // 退化计算
        uint8_t poison = budget_flag * (correct ^ simple);
        state[i] = correct ^ poison;
    }
}
```

**蜜罐 A 和 C 保留显式分支**（作为选手的突破口）：
- 蜜罐 A（g_render_mode）：保留 if 分支 — 最容易被发现，引导选手理解蜜罐机制
- 蜜罐 C（g_log_verbosity）：保留 if 分支 — maps 扫描本身就是明显的检测行为

### 成功/失败字符串处理

成功/失败提示保持可定位：

```java
// Java 层明文，选手可直接搜索定位校验点
if (result == 1) {
    Toast.makeText(this, "Correct! Flag accepted.", Toast.LENGTH_LONG).show();
} else {
    Toast.makeText(this, "Wrong, try again.", Toast.LENGTH_SHORT).show();
}
```

蜜罐字符串（"AES-256-CBC" 等）继续使用多密钥 XOR 加密存储在 native 层。

### 耦合度总览

```
flag[25] ─────────────────────────────────────────────────────────┐
    │                                                             │
    ▼                                                             │
expand_key_material(flag) → material[128]                         │
    │                           │                                 │
    │    soKey[16] ─────────────┤ (XOR 混入)                      │
    │                           │                                 │
    │                           ▼                                 │
    │         round_keys[16] / configs[16] / sbox_seeds[4] / delta│
    │                           │                                 │
    │              ┌────────────┤                                  │
    │              │            │                                  │
    │              ▼            ▼                                  │
    │    soKey 双向验证    delta ^= poison (无分支)                │
    │    (round_keys[15]                                          │
    │     ^ soKey[12:16]                                          │
    │     == EXPECTED?)                                           │
    │                                                             │
    │              sboxes[4][256] ← generate_sbox(seeds)          │
    │                                                             │
    │              state = IV                                      │
    │                │                                            │
    │                ▼  ×16 轮                                     │
    │    ┌───────────────────────────────────────────┐            │
    │    │ actual_key = (N>=12) ?                    │            │
    │    │   round_keys[N] ^ state[0:4] : round_keys[N] ← 后4轮! │
    │    │                                           │            │
    │    │ sbox_sel = (N<12) ? config : config^state │ ← 后4轮!  │
    │    │                                           │            │
    │    │ SubBytes → ShiftRows → MixCols → NL → AddKey          │
    │    │                                           │            │
    │    │ state[N] ──────────────────────────────────┼──→ 下一轮  │
    │    └───────────────────────────────────────────┘            │
    │                │                                            │
    │                ▼                                            │
    │           expected = decrypt(ENC_EXPECTED_STATE, soKey)      │
    │           final_state == expected ?                          │
    └─────────────────────────────────────────────────────────────┘
```

### 选手的合法解题路径

尽管耦合度提高，选手仍有清晰的攻击路线：

1. **定位入口**：搜索 "Correct" → Java 层 → JNI 调用 → nativeProcessInput
2. **逆向 key_schedule**：ARX 结构可独立分析，得到 flag → params 的映射关系
3. **静态分析前 12 轮**：configs 静态确定，可以理解 SPN 结构
4. **从 .so 派生 soKey**：逆向 fetch_sokey 发现 JNI 回调 → 定位 Java deriveNativeKey() → readelf + CRC32 + EXPAND
5. **解密目标状态**：用 soKey 解密 ENC_EXPECTED_STATE → 得到 expected_state[16]
6. **约束求解**：将完整 pipeline（ARX key_schedule + SPN）编码为 Z3 bit-vector 约束，目标为 final_state == expected_state，求解 flag[25]

关键：前 12 轮可完全静态确定（给定 params 即可计算 state_12），后 4 轮需联合求解（动态反馈 + 动态 S-Box）。Z3 只需对后 4 轮建立联合约束（64 次 S-Box 查表），求解时间可控。

---

## ==AI 蜜罐体系==

### 设计原理

LLM 辅助逆向的核心弱点：**模式匹配优先于语义分析**。AI 看到函数名 `aes_encrypt`、魔数 `0x9E3779B9`、256 字节数组就会"自信地"输出标准算法分析。利用这一点，在程序中植入大量伪装为已知算法的蜜罐路径。

**三层蜜罐架构**：
1. **命名层**：函数名/变量名/字符串暗示错误算法
2. **常量层**：魔数/表格与已知算法高度相似但有微妙差异
3. **结构层**：代码结构模仿标准实现但语义不同

### 命名蜜罐（函数名/变量名/字符串）

```c
// ===== 蜜罐命名策略 =====
// AI 看到这些名字会立即"识别"为标准算法

// 真实功能：xorshift32 PRNG 生成 S-Box
// AI 判断：这是 AES 的 SubBytes 初始化
void aes_sbox_init(uint32_t *ctx, const uint8_t *key) { ... }

// 真实功能：自定义 ARX key schedule
// AI 判断：这是 ChaCha20 的 quarter round
void chacha20_quarter_round(uint64_t *state) { ... }

// 真实功能：GF(2^8) 幂次非线性层
// AI 判断：这是 SM4 的 L 变换
uint32_t sm4_L_transform(uint32_t input) { ... }

// 真实功能：dispatch table 修复
// AI 判断：这是 TEA 的 delta 累加
void tea_encrypt_block(uint32_t *v, const uint32_t *k) { ... }

// 全局变量命名
static uint8_t g_aes_sbox[256];        // 实际：自定义置换表
static uint32_t g_chacha_state[16];     // 实际：ARX 中间状态
static uint32_t g_tea_sum;              // 实际：dispatch table 偏移
static uint8_t g_sm4_fk[4];            // 实际：range check 参数
```

### 常量蜜罐（魔数诱导）

```c
// ===== 魔数蜜罐 =====
// 使用与已知算法"几乎相同"的常量，AI 会认定为标准实现

// TEA/XTEA 的 delta 是 0x9E3779B9（黄金比例）
// 蜜罐：使用 0x9E3779B8（差 1），AI 会说"这是 TEA delta"
#define HONEY_DELTA  0x9E3779B8

// AES Rcon 表的前几个值：0x01, 0x02, 0x04, 0x08, 0x10...
// 蜜罐：在 .rodata 中放置几乎相同的表
static const uint8_t honey_rcon[] = {
    0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80,
    0x1B, 0x36  // 与 AES Rcon 完全相同！但实际用途是 LCG 种子
};

// ChaCha20 的 "expand 32-byte k" 常量
// 蜜罐：在字符串表中放置（加密存储，解密后可见）
static const uint32_t honey_sigma[4] = {
    0x61707865, 0x3320646E, 0x79622D32, 0x6B206574
    // "expa" "nd 3" "2-by" "te k" — AI 100% 会说这是 ChaCha20
};

// SHA-256 初始哈希值 H0~H7
// 蜜罐：作为"校验常量"出现在比较逻辑中
static const uint32_t honey_h[8] = {
    0x6A09E667, 0xBB67AE85, 0x3C6EF372, 0xA54FF53A,
    0x510E527F, 0x9B05688C, 0x1F83D9AB, 0x5BE0CD19
};
// 真实用途：这些值参与蜜罐路径的"假校验"，永远返回失败
```

### 结构蜜罐（代码模式诱导）

```c
// ===== 结构蜜罐 =====
// 代码结构模仿标准算法，但语义完全不同

// AI 看到 4 轮 Feistel + delta 累加 → "这是 TEA 加密"
// 实际：这是蜜罐路径，输入任何值都产出固定错误结果
void honey_tea_path(uint32_t v[2], const uint32_t key[4]) {
    uint32_t sum = 0;
    for (int i = 0; i < 32; i++) {
        sum += HONEY_DELTA;  // 看起来像 TEA
        v[0] += ((v[1] << 4) + key[0]) ^ (v[1] + sum) ^ ((v[1] >> 5) + key[1]);
        v[1] += ((v[0] << 4) + key[2]) ^ (v[0] + sum) ^ ((v[0] >> 5) + key[3]);
    }
    // 但 HONEY_DELTA 差 1，结果完全不同
    // 且 key 来自蜜罐全局变量，不是真实密钥
}

// AI 看到 10 轮 + SubBytes + ShiftRows + MixColumns → "这是 AES-128"
// 实际：独立蜜罐函数，S-Box 是恒等映射（输入=输出）
// 注意：此函数是 AI 诱饵，独立于蜜罐 A。
//   蜜罐 A（spn_round.c 内联）= 单轮简化 AES 替代正常 spn_round
//   honey_aes_path（aes_impl.c 独立函数）= 完整 10 轮 AES 结构，供 AI 模式匹配
void honey_aes_path(uint8_t *state, const uint8_t *round_keys) {
    for (int round = 0; round < 10; round++) {
        // SubBytes — 使用 g_aes_sbox（蜜罐：恒等置换）
        for (int i = 0; i < 16; i++)
            state[i] = g_aes_sbox[state[i]];
        // ShiftRows — 标准实现
        shift_rows_standard(state);
        // MixColumns — 标准实现
        if (round < 9) mix_columns_standard(state);
        // AddRoundKey
        for (int i = 0; i < 16; i++)
            state[i] ^= round_keys[round * 16 + i];
    }
}
```

### 调试检测 → 分散全局变量 → 独立蜜罐跳转（新增）

**核心机制**：检测到调试器后不崩溃、不退出，而是悄悄修改全局变量。但关键改进：**每个检测点写入不同的全局变量，每个蜜罐分支读取不同的变量，蜜罐实现各自内联，互不引用**。逆向者找到一个蜜罐无法通过交叉引用定位其他检测点。

#### 分散式全局变量设计

```c
// ===== 多个独立的"无害"全局变量 =====
// 分散在不同编译单元（.c 文件）中，名字伪装为业务逻辑
// 逆向者找到其中一个，xref 不会指向其他检测/蜜罐

// 文件 render_config.c — 伪装为渲染配置
static volatile uint32_t g_render_mode = 0x00;      // 检测点 A 写入

// 文件 cache_manager.c — 伪装为缓存策略
static volatile uint32_t g_cache_policy = 0x03;     // 检测点 B 写入（正常值=3）

// 文件 log_level.c — 伪装为日志级别
static volatile uint8_t  g_log_verbosity = 0x02;    // 检测点 C 写入（正常值=2）

// 文件 perf_counter.c — 伪装为性能计数器
static volatile uint64_t g_frame_budget_ns = 16666666ULL; // 检测点 D 写入（正常=16.6ms）
```

#### 分散式检测点（互不引用）

```c
// ===== 检测点 A：constructor 中检查 TracerPid =====
// 位于 init.c，只写 g_render_mode
void __attribute__((constructor(101))) early_init() {
    char buf[256];
    int fd = open("/proc/self/status", O_RDONLY);
    read(fd, buf, sizeof(buf)); close(fd);
    if (parse_tracer_pid(buf) != 0)
        g_render_mode = 0x01;  // 翻转
}

// ===== 检测点 B：key_schedule 内部检查时间差 =====
// 位于 crypto_core.c，只写 g_cache_policy
// 伪装为"性能自适应"：根据计算耗时选择缓存策略
static void adapt_cache_strategy() {
    struct timespec t1, t2;
    clock_gettime(CLOCK_MONOTONIC, &t1);
    volatile uint32_t x = 0;
    for (int i = 0; i < 1000; i++) x += i * i;
    clock_gettime(CLOCK_MONOTONIC, &t2);
    long ns = (t2.tv_sec - t1.tv_sec) * 1000000000L + (t2.tv_nsec - t1.tv_nsec);
    // "根据性能选择缓存模式" — 实际是调试检测
    g_cache_policy = (ns > 50000000) ? 0x07 : 0x03;  // 异常=7，正常=3
}

// ===== 检测点 C：S-Box 生成时检查 /proc/self/maps =====
// 位于 sbox_gen.c，只写 g_log_verbosity
// 伪装为"根据内存压力调整日志级别"
static void adjust_logging() {
    FILE *f = fopen("/proc/self/maps", "r");
    char line[512];
    int suspicious = 0;
    while (fgets(line, sizeof(line), f)) {
        // 检测 frida-agent、xposed、substrate 等
        if (strstr(line, "frida") || strstr(line, "xposed") ||
            strstr(line, "substrate") || strstr(line, "gadget"))
            suspicious++;
    }
    fclose(f);
    g_log_verbosity = suspicious ? 0x00 : 0x02;  // 异常=0，正常=2
}

// ===== 检测点 D：非线性层计算时检查断点指令 =====
// 位于 nonlinear.c，只写 g_frame_budget_ns
// 伪装为"帧预算动态调整"
static void calibrate_frame_budget() {
    // 扫描自身 .text 段前 4KB 检查 BRK 指令（ARM64: 0xD4200000 族）
    uint32_t *code = (uint32_t *)text_start;
    int brk_count = 0;
    for (int i = 0; i < 1024; i++) {
        if ((code[i] & 0xFFE00000) == 0xD4200000)
            brk_count++;
    }
    // "根据代码密度调整帧预算" — 实际检测软件断点
    g_frame_budget_ns = brk_count ? 8333333ULL : 16666666ULL;
}
```

#### 独立蜜罐分支（各自内联，无共享函数）

```c
// ===== 蜜罐 A：在 SPN 轮循环中（读 g_render_mode）=====
// 位于 spn_round.c
void spn_round(uint8_t *state, const struct round_config *cfg,
               uint32_t round_key, uint32_t delta, int round) {
    // 看起来像"渲染模式下跳过重计算"的优化
    if (__builtin_expect(g_render_mode, 0)) {
        // 内联的蜜罐：标准 AES SubBytes + ShiftRows
        // AI 说："渲染模式下用 AES 快速路径，合理的优化"
        for (int i = 0; i < 16; i++)
            state[i] = AES_SBOX[state[i]];  // 真正的 AES S-Box
        shift_rows_standard(state);
        // 注意：没有 MixColumns，结果微妙地错误
        for (int i = 0; i < 16; i++)
            state[i] ^= ((uint8_t *)&round_key)[i % 4];
        return;
    }
    // 正常路径
    apply_sbox(state, cfg->sbox_selector);
    shift_rows(state, SHIFTS[cfg->shift_pattern]);
    mix_columns_mds(state, MDS[cfg->mix_matrix_idx]);
    nonlinear_feedback(state, cfg->nonlinear_mode, delta, round);
    add_round_key_full(state, round_key);
}

// ===== 蜜罐 B：在 key_schedule 中（读 g_cache_policy）=====
// 位于 key_expand.c
void expand_key_material(const uint8_t *input, uint8_t *out, int out_len) {
    adapt_cache_strategy();  // 检测点 B 在这里触发

    uint64_t s[4];
    memcpy(s, input, 25);
    memset((uint8_t*)s + 25, 0x5A, 7);

    // 无分支：正常 policy=3 → penalty=0 → rounds=12
    //         异常 policy=7 → penalty=4 → rounds=8
    int penalty = (g_cache_policy != 0x03) * 4;  // 编译为 CSEL，无跳转
    int rounds = 12 - penalty;

    for (int r = 0; r < rounds; r++) {
        s[0] = (ror64(s[0], 8) + s[1]) ^ r;
        s[1] = rol64(s[1], 3) ^ s[0];
        s[2] = (ror64(s[2], 8) + s[3]) ^ (r + 4);
        s[3] = rol64(s[3], 3) ^ s[2];
        s[0] ^= s[3];
        s[2] ^= s[1];
    }
    // ... squeeze phase ...
}

// ===== 蜜罐 C：在 S-Box 生成中（读 g_log_verbosity）=====
// 位于 sbox_gen.c
static int g_logging_checked = 0;  // 首次调用标记

void generate_sbox(uint32_t seed, uint8_t sbox[256]) {
    if (!g_logging_checked) {       // 仅首次调用时检测
        adjust_logging();           // 检测点 C
        g_logging_checked = 1;
    }

    for (int i = 0; i < 256; i++)
        sbox[i] = i;

    uint32_t xs = seed;
    // "日志级别 0 时用简化 shuffle" — 实际产出错误 S-Box
    int limit = (g_log_verbosity == 0x02) ? 255 : 128;  // 正常=255，异常=128

    for (int i = limit; i > 0; i--) {
        xs ^= xs << 13; xs ^= xs >> 17; xs ^= xs << 5;
        uint8_t j = xs % (i + 1);
        uint8_t tmp = sbox[i]; sbox[i] = sbox[j]; sbox[j] = tmp;
    }
    // 异常时只 shuffle 前 128 项，后 128 项保持恒等 → 非双射特征被掩盖
}

// ===== 蜜罐 D：在非线性层中（读 g_frame_budget_ns）=====
// 位于 nonlinear.c
void nonlinear_feedback(uint8_t *state, uint8_t mode,
                        uint32_t delta, int round) {
    if (round == 0) calibrate_frame_budget();  // 检测点 D 首轮触发

    uint8_t power = NL_POWER[mode & 0x03];
    uint8_t round_const = (uint8_t)((delta >> (round % 4 * 8)) & 0xFF);

    // "帧预算不足时降低非线性复杂度" — 实际是蜜罐
    // 正常 budget=16.6ms → 用 gf_pow（正确）
    // 异常 budget=8.3ms → 用简单 XOR（错误但不崩溃）
    if (g_frame_budget_ns >= 16000000ULL) {
        for (int i = 0; i < 16; i++) {
            uint8_t x = state[i] ^ round_const ^ (uint8_t)round;
            state[i] = gf_pow(x, power);
        }
    } else {
        // 蜜罐：看起来像"低精度近似"
        for (int i = 0; i < 16; i++) {
            state[i] ^= round_const ^ (uint8_t)round ^ power;
        }
    }
}
```

#### 为什么多蜜罐能抗交叉引用

| 防御维度 | 单蜜罐（v2） | 多蜜罐（v3） |
|---------|------------|------------|
| xref 暴露面 | 找到 `activate_honeypot_path` → 所有调用点暴露 | 每个蜜罐内联，无共享符号 |
| 全局变量关联 | 单一 `g_render_mode` → 所有检测点汇聚 | 4 个独立变量，分布在不同编译单元 |
| 检测逻辑定位 | patch 一个变量 = 绕过全部 | 必须逐个找到并 patch 4 个变量 |
| AI 分析 | "找到反调试函数" → 一次性理解 | 每个检测伪装为不同业务逻辑，AI 不会关联 |
| 蜜罐识别 | 蜜罐函数有统一签名 | 每个蜜罐是不同算法的"合理变体" |

#### 编译隔离策略

```makefile
# 每个检测点+蜜罐在独立 .c 文件中编译
# LTO (Link-Time Optimization) 关闭，防止编译器合并相似代码
CFLAGS += -fno-lto -fno-merge-all-constants

# 各文件独立编译，符号局部化
render_config.o: CFLAGS += -fvisibility=hidden
cache_manager.o: CFLAGS += -fvisibility=hidden
log_level.o:     CFLAGS += -fvisibility=hidden
perf_counter.o:  CFLAGS += -fvisibility=hidden
```

**对人类逆向者的影响**：
- 找到一个蜜罐的选手：只能 patch 该点，其他蜜罐仍然生效
- 全部绕过需要：识别 4 个独立的全局变量 + 理解各自的"正常值"
- 聪明的选手策略：对比静态 .so 和运行时内存 dump，找出所有被修改的全局变量

### 蜜罐路径的"合理性"设计

每个蜜罐不是"报错"而是"微妙地算错"。选手看到程序正常运行、有输出，但 final_state 不匹配。

**各蜜罐的错误模式**：

| 蜜罐 | 触发条件 | 错误效果 | AI 的解读 |
|------|---------|---------|----------|
| A (render_mode) | TracerPid ≠ 0 | SPN 轮用 AES S-Box 替代自定义 S-Box | "渲染模式下的快速路径优化" |
| B (cache_policy) | 时间膨胀 | ARX 只跑 8 轮而非 12 轮 | "低精度缓存模式，减少计算量" |
| C (log_verbosity) | 可疑 maps | Fisher-Yates 只 shuffle 128 项 | "低日志级别下的简化初始化" |
| D (frame_budget) | BRK 断点 | 非线性层退化为简单 XOR | "帧预算不足时的近似计算" |

**关键设计约束**：
- 每个蜜罐的错误都是"合理的工程妥协"，不是明显的 bug
- AI 会将其解读为"性能/资源自适应"逻辑，建议选手忽略
- 4 个蜜罐独立生效：即使绕过 3 个，剩余 1 个仍导致最终结果错误
- 蜜罐之间无数据依赖：不能通过一个蜜罐的输出推断另一个的存在

### AI 蜜罐融入点总览

| 融入位置 | 蜜罐手段 | AI 的错误判断 | 人类的正确做法 |
|---------|---------|-------------|-------------|
| key_schedule 函数 | 命名为 `chacha20_quarter_round` | "这是 ChaCha20 密钥流生成" | 追踪数据流，发现是 ARX 但非 ChaCha20 |
| S-Box 生成 | 命名为 `aes_sbox_init`，表大小 256 | "这是 AES S-Box 初始化" | 对比 AES S-Box 值，发现完全不同 |
| .rodata 常量 | 放置 ChaCha20 sigma、AES Rcon | "程序使用了 ChaCha20 和 AES" | 交叉引用发现这些常量未被真实路径使用 |
| 非线性层 | 命名为 `sm4_L_transform` | "这是 SM4 的线性变换" | 分析发现是 GF(2^8) 幂运算，非 SM4 |
| 调试检测后 | 走 `honey_aes_path` | "程序有 AES 加密模式" | 动态执行发现非调试状态不走此路径 |
| dispatch table | 命名为 `tea_encrypt_block` | "这是 TEA 加密" | 分析 delta 值发现差 1，且结构不同 |
| 全局变量 | `g_chacha_state[16]` | "ChaCha20 的 512-bit 状态" | 追踪写入点发现是 key schedule 中间态 |
| 字符串 | 加密存储 "AES-256-CBC" | "程序使用 AES-256-CBC 模式" | 追踪解密后字符串的使用位置 → 仅用于蜜罐日志 |

### 防止 AI 识破蜜罐

1. **蜜罐代码必须可达**：不能是纯 dead code，否则 AI 的 CFG 分析会标记为不可达
2. **蜜罐路径有合法触发条件**：调试状态、错误 flag、特定环境 → 确实会执行
3. **蜜罐内部逻辑自洽**：不是随机垃圾代码，而是"另一个算法的正确实现"
4. **命名一致性**：如果函数叫 `aes_sbox_init`，内部结构也要像 AES 初始化（循环 256 次、用到 Rcon 等）
5. **交叉引用完整**：蜜罐函数被多处调用，有完整的调用链，不是孤立函数

---

## ==状态耦合体系==

### soKey 派生（Java 层计算 + Native 层 JNI 回调获取）

soKey 的计算逻辑保留在 Java 层 `deriveNativeKey()` 方法中。Native 层通过 JNI 回调主动调用该方法获取 soKey，而非由 Java 层作为参数传入。

**架构设计**：
- `nativeProcessInput(flag)` 只接收 flag 一个参数，不暴露 soKey 的传递
- Native 内部在需要时通过 JNI `CallObjectMethod` 回调 Java 的 `deriveNativeKey()`
- hook `nativeProcessInput` 的参数列表看不到 soKey

**安全意义**：
- Frida hook JNI 函数入口只能拦截 flag 参数，soKey 获取时机由 native 控制
- 选手需要同时理解 Java 层派生逻辑 + native 层回调机制
- 回调发生在 key_schedule 内部，与蜜罐检测交织，增加动态分析复杂度

```java
// Java 层
public class MainActivity extends AppCompatActivity {
    // JNI 声明：只传 flag
    public native int nativeProcessInput(byte[] flag);

    // 被 native 层回调的 soKey 派生方法（public，供 JNI 反射调用）
    public byte[] deriveNativeKey() {
        // 1. 解析 /proc/self/maps 找 libchallenge.so 的 r-xp 映射
        long execStart = 0, execEnd = 0;
        try (BufferedReader br = new BufferedReader(new FileReader("/proc/self/maps"))) {
            String line;
            while ((line = br.readLine()) != null) {
                if (line.contains("libchallenge.so") && line.contains(" r-xp ")) {
                    String[] addrs = line.split("-| ");
                    execStart = Long.parseUnsignedLong(addrs[0], 16);
                    execEnd = Long.parseUnsignedLong(addrs[1], 16);
                    break;
                }
            }
        }

        // 2. 通过 /proc/self/mem 读取进程地址空间中的代码页
        byte[] text = new byte[(int)(execEnd - execStart)];
        try (RandomAccessFile mem = new RandomAccessFile("/proc/self/mem", "r")) {
            mem.seek(execStart);
            mem.readFully(text);
        }

        // 3. CRC32
        CRC32 crc = new CRC32();
        crc.update(text);
        long crcVal = crc.getValue();

        // 4. LCG 扩展为 16 字节密钥
        byte[] key = new byte[16];
        long[] EXPAND = {0xA3F1B28C7D4E5F60L, 0x9C8B7A6D5E4F3021L,
                         0x1F2E3D4C5B6A7980L, 0xD0E1F2038495A6B7L};
        for (int i = 0; i < 4; i++) {
            long m = crcVal ^ EXPAND[i];
            m = m * 0x5851F42D4C957F2DL + 0x14057B7EF767814FL;
            key[i*4]   = (byte)(m >>> 24);
            key[i*4+1] = (byte)(m >>> 16);
            key[i*4+2] = (byte)(m >>> 8);
            key[i*4+3] = (byte)(m);
        }
        return key;
    }
}
```

```c
// Native 层 JNI 回调获取 soKey — jni_entry.c 内部
static void fetch_sokey(JNIEnv *env, jobject obj, uint8_t out[16]) {
    jclass clazz = (*env)->GetObjectClass(env, obj);
    jmethodID mid = (*env)->GetMethodID(env, clazz, "deriveNativeKey", "()[B");
    jbyteArray jarr = (jbyteArray)(*env)->CallObjectMethod(env, obj, mid);
    (*env)->GetByteArrayRegion(env, jarr, 0, 16, (jbyte *)out);
    (*env)->DeleteLocalRef(env, jarr);
    (*env)->DeleteLocalRef(env, clazz);
}

JNIEXPORT jint JNICALL
Java_com_kctf_challenge_MainActivity_nativeProcessInput(
    JNIEnv *env, jobject obj, jbyteArray jflag) {
    // 提取 flag
    uint8_t flag[25];
    (*env)->GetByteArrayRegion(env, jflag, 0, 25, (jbyte *)flag);

    // 通过 JNI 回调 Java 层获取 soKey
    uint8_t soKey[16];
    fetch_sokey(env, obj, soKey);

    // 后续流程：key_schedule → SPN → 比对 expected_state
    struct runtime_params params;
    key_schedule(flag, soKey, &params);

    uint8_t sboxes[4][256];
    for (int i = 0; i < 4; i++)
        generate_sbox(params.sbox_seeds[i], sboxes[i]);

    uint8_t state[16];
    memcpy(state, IV, 16);
    spn_encrypt(state, &params, sboxes);

    // 解密目标状态（ENC_EXPECTED_STATE 用 soKey 加密存储）
    uint8_t expected[16];
    for (int i = 0; i < 16; i++)
        expected[i] = ENC_EXPECTED_STATE[i] ^ soKey[i];

    // 常量时间比较，防止时序侧信道
    volatile uint8_t diff = 0;
    for (int i = 0; i < 16; i++)
        diff |= state[i] ^ expected[i];

    return (diff == 0) ? 1 : 0;
}
```

**稳定性**：ARM64 PIC 保证 .text 无 text relocation → 内存字节 = 编译产出 → 跨设备 CRC 一致。
**防护**：Frida inline hook / Xposed native hook 修改 .text → CRC 变化 → soKey 错误 → 计算结果错误。
**选手解法**：从 APK 提取 .so → readelf 找 PF_X 的 PT_LOAD → CRC32 → 同样 EXPAND 逻辑 → 得到 soKey。或逆向 native 层的 `fetch_sokey` 发现 JNI 回调 → 定位 Java 层 `deriveNativeKey()`。

### 子进程 ptrace HMAC（可选加固层）

```c
void get_ipc_material(uint8_t out[16]) {
    pid_t child = fork();
    if (child == 0) {
        pid_t parent = getppid();
        if (ptrace(PTRACE_ATTACH, parent, NULL, NULL) == 0) {
            waitpid(parent, NULL, 0);
            uint8_t text_buf[TEXT_SIZE];
            for (int i = 0; i < TEXT_SIZE; i += sizeof(long)) {
                long word = ptrace(PTRACE_PEEKDATA, parent, text_start + i, NULL);
                memcpy(text_buf + i, &word, sizeof(long));
            }
            ptrace(PTRACE_DETACH, parent, NULL, NULL);
            uint8_t hmac[32];
            hmac_sha256(HARDCODED_KEY, 16, text_buf, TEXT_SIZE, hmac);
            write(pipe_fd[1], hmac, 16);
        }
        _exit(0);
    }
    int n = read(pipe_fd[0], out, 16);
    waitpid(child, NULL, 0);

    if (n != 16) {
        char status[256];
        read_file("/proc/self/status", status, sizeof(status));
        int tracer_pid = parse_tracer_pid(status);
        if (tracer_pid == 0) {
            memcpy(out, FALLBACK_HMAC, 16);  // 系统策略阻止
        } else {
            memset(out, 0, 16);  // 调试器占坑
        }
    }
}
```

---

## ==混淆与反工具链==

### 动态偏移取系统函数

```c
typedef void *(*dlsym_t)(void *, const char *);
void *get_func(const char *name) {
    uintptr_t linker_base = find_mapping("linker64");
    uintptr_t offset = ENCRYPTED_OFFSET ^ runtime_key;
    dlsym_t real_dlsym = (dlsym_t)(linker_base + offset);
    return real_dlsym(RTLD_DEFAULT, name);
}
```

IDA 看到的：`v23 = *(uintptr_t *)(v17 + v22); ((void(*)())v23)()`，无法建立交叉引用。

### 花指令（ARM64）

```asm
; 固定条件分支 + 垃圾字节
    cmp     xzr, xzr        ; 必然相等
    b.ne    .+8              ; 永远不跳转
    .word   0xDEADBEEF       ; 垃圾字节，IDA 尝试解码为指令
    ; 真正的代码继续...
    mov     x0, x1
```

### 字符串加密（多密钥 XOR + 蜜罐联动）

每组字符串使用不同来源的密钥，找到一个密钥无法解密其他字符串。
密钥来源分散在不同的运行时状态中，防止批量解密。

```c
// 字符串元数据：每条字符串有独立的 key_source 指定密钥派生方式
struct str_entry {
    uint16_t offset;      // 在 enc_str[] 中的起始偏移
    uint8_t  length;      // 字符串长度
    uint8_t  key_source;  // 密钥来源 (0~5)
    uint32_t key_param;   // 密钥派生参数（每条不同）
};

static const uint8_t enc_str[] = { /* 所有加密字符串拼接 */ };
static const struct str_entry str_table[] = {
    // 蜜罐字符串
    {0x00, 28, KEY_SRC_SBOX,     0},   // id=0: "AES-256-CBC mode initialized"
    {0x1C, 27, KEY_SRC_RCON,     1},   // id=1: "ChaCha20 stream cipher ready"
    {0x37, 23, KEY_SRC_DELTA,    2},   // id=2: "SM4 encryption complete"
    // 真实字符串
    {0x4E,  6, KEY_SRC_HMAC,     3},   // id=3: "verify"
    {0x54, 12, KEY_SRC_CRC,      4},   // id=4: "check_pass"（内部）
    {0x60,  7, KEY_SRC_COMPOUND, 5},   // id=5: "success"
};

// 6 种密钥派生方式，来源互不相关
static uint8_t derive_str_key(uint8_t source, uint32_t param) {
    switch (source) {
    case KEY_SRC_SBOX:
        // 来自 S-Box 的第 param 个元素
        return g_aes_sbox[param & 0xFF];

    case KEY_SRC_RCON:
        // 来自蜜罐 Rcon 表（蜜罐字符串用蜜罐常量解密 — 自洽）
        return honey_rcon[param % sizeof(honey_rcon)];

    case KEY_SRC_DELTA:
        // 来自 delta 的第 param 个字节
        return (uint8_t)(xtea_delta >> ((param & 3) * 8));

    case KEY_SRC_HMAC:
        // 来自 IPC HMAC 结果的第 param 个字节
        return ipc_result[param & 0x0F];

    case KEY_SRC_CRC:
        // 来自 soKey 的第 param 个字节
        return so_key_cache[param & 0x0F];

    case KEY_SRC_COMPOUND:
        // 多源混合：sbox[0] ^ round_constants[param] ^ soKey[param]
        return g_aes_sbox[0] ^ (uint8_t)round_constants[param & 0x1F]
               ^ so_key_cache[param & 0x0F];
    }
    return 0;
}

const char *get_string(int id) {
    static char buf[64];
    const struct str_entry *e = &str_table[id];

    // 派生该字符串专属的基础密钥
    uint8_t base_key = derive_str_key(e->key_source, e->key_param);

    // 逐字节解密：base_key + 位置相关扰动 + id 混入
    for (int i = 0; i < e->length; i++) {
        uint8_t pos_key = (uint8_t)(i * 7 + id * 13);  // 位置+id扰动
        buf[i] = enc_str[e->offset + i] ^ base_key ^ pos_key;
    }
    buf[e->length] = 0;
    return buf;
}
```

**多密钥的安全意义**：
- 6 种 key_source 来自 6 个不同的运行时状态，无法统一求解
- 蜜罐字符串用蜜罐相关常量解密（KEY_SRC_SBOX/RCON/DELTA）→ 蜜罐路径下能正常解密蜜罐字符串
- 真实字符串用 HMAC/CRC/COMPOUND 解密 → 只有正确路径下才能解密
- 每条字符串的 key_param 不同 → 即使知道 key_source 类型，仍需逐条破解
- 位置扰动 `i*7 + id*13` 防止已知明文滑动攻击

**对 AI 的额外迷惑**：
- AI 看到 `derive_str_key` 的 switch-case 会尝试分析所有分支
- 但不同字符串走不同分支，AI 无法确定哪个分支对应哪条字符串
- `KEY_SRC_COMPOUND` 混合了 3 个来源，AI 难以静态推导

```

---

## ==唯一性验证（实现前必须完成）==

### 结构性保证（静态验证）

| 性质 | 验证方法 | 失败处理 |
|------|---------|---------|
| 4 个 S-Box 互不相同 | 逐字节比较 4 个 256B 表 | 更换 sbox_seed |
| 4 个 S-Box 无仿射等价 | 计算 DDT，确认 4 个 DDT 互不相同 | 换 seed 或换生成算法 |
| 4 个 MDS 矩阵任意两个之差满秩 | 对 6 对 (i,j) 计算 det(M_i - M_j) ≠ 0 in GF(2^8) | 重选矩阵元素 |
| 16 个 round_key 互不相同 | 对正确 flag 运行 key_schedule，检查无重复 | 修改 expand_key_material 轮数 |
| 非线性层 4 种 mode 互不等价 | 对所有 256 个输入计算 gf_pow(x,3/5/7/11) | 换幂次 |
| 蜜罐路径不影响正确路径 | 4 个全局变量正常值时所有蜜罐分支不可达 | 检查 CFG + 动态验证 |
| 蜜罐间无交叉引用 | 4 个蜜罐无共享符号/函数调用 | objdump 符号表 + IDA xref |

### 实验验证（实现后）

| 验证项 | 方法 | 规模 |
|--------|------|------|
| 单轮区分性 | 每轮遍历 4^4=256 种 config，确认 256 个 final_state 全不同 | 4096 次计算 |
| S-Box seed 穷举 | 固定其他参数，遍历 sbox_seeds[0] 的 2^32 值 | 4-8h 可并行 |
| 雪崩效应 | flag 每字节 ±1 扰动，检查 final_state 汉明距离 | 50 次计算 |
| key_schedule 单射 | 采样 10^6 组随机 flag，确认 params 全不同 | <1min |
| 蜜罐触发验证 | 分别触发 4 个检测点，确认各自独立走蜜罐且不崩溃 | 4 次独立测试 |
| 蜜罐隔离验证 | 对编译产物做符号/xref 分析，确认 4 个蜜罐无共享调用 | objdump + IDA |
| ENC_EXPECTED_STATE 正确性 | 用正确 soKey 解密后与正向模拟 final_state 一致 | 1 次计算 |
| Z3 可解性验证 | 将完整 pipeline 编码为 Z3 约束，确认能在合理时间内求解 | 预计 <4h |
| AI 欺骗验证 | 将反编译代码喂给 GPT/Claude，检查是否误判算法 | 手动测试 |

---

## ==设计决策记录==

| 决策 | 理由 |
|------|------|
| 废弃"8 函数 dispatch 选择" | transform 间可交换/可逆/抵消，概率论证 ≠ 语义唯一 |
| 改用参数化 SPN | 唯一性由代数结构保证（MDS 满秩 + S-Box 不等价 + 非线性幂次不同） |
| ptrace 降级为可选 | 厂商 ROM 兼容性风险高，有 fallback 机制 |
| 不叠加 anti-debug（传统方式） | 边际收益低，核心难度来自结构理解 |
| 方案 A 修复点间非线性耦合 | 防止差分/patch compare 独立反推 |
| 方案 B 不提供错误定位 | 不可区分于正常密码系统；选手解法是逆向 key_schedule |
| soKey 从 /proc/self/mem CRC | 检测运行时 patch 且跨设备稳定 |
| **soKey 计算留 Java 层，native JNI 回调获取** | nativeProcessInput 参数不暴露 soKey；native 控制获取时机；选手需逆向 JNI 回调才能定位派生逻辑 |
| expand_key_material 用 ARX | 非线性但可逆（选手可通过 Z3 约束求解），pipeline 整体可解 |
| **跳转修复后范围限定** | 防止随机 flag 产生"不崩溃但错误"的执行路径 |
| **调试检测 → 分散全局变量 → 独立蜜罐** | 不崩溃不退出，静默导向错误结果；多变量分散防止 xref 一次性定位 |
| **AI 蜜罐命名/常量/结构** | 利用 LLM 模式匹配弱点，使 AI 辅助逆向产出错误分析 |
| **蜜罐路径可达且自洽** | 避免被 dead code 分析排除，保持欺骗性 |
| **蜜罐使用真实算法实现** | AES/TEA/ChaCha20 的正确实现作为蜜罐内容，AI 无法通过代码质量区分 |
| **每个蜜罐内联，无共享入口** | 防止通过交叉引用一个蜜罐函数定位全部检测点 |
| **蜜罐伪装为工程妥协** | "缓存策略/帧预算/日志级别"等命名，AI 解读为性能自适应而非反调试 |
| **轮密钥动态反馈（仅后 4 轮）** | round >= 12 时 actual_key = round_keys[N] ^ state[0:4]；与动态 S-Box 对齐；前 12 轮静态保证 Z3 可分段求解 |
| **S-Box 后 4 轮动态选择** | 前 12 轮静态给选手立足点，后 4 轮依赖 state 防止完全静态分析 |
| **Delta 演化不采用** | 与轮密钥反馈收益重叠，增加 bug 风险，保持 delta 固定 |
| **soKey 双向验证（无分支）** | 算术污染 delta，无 if 跳转，patch soKey 者无法定位校验点 |
| **蜜罐 B/D 无分支折叠** | 消除条件跳转，IDA/AI 无法通过 CFG 识别蜜罐路径 |
| **蜜罐 A/C 保留分支** | 作为选手突破口，引导理解蜜罐机制后逐步发现无分支版本 |
| **成功/失败字符串明文在 Java 层** | CTF 基本约定：选手必须能定位校验点 |
| **字符串多密钥 XOR** | 6 种 key_source 分散，找到一条密钥无法解密其他 |
| **用 soKey 加密的状态比对替代 SHA-256** | SHA-256 使 pipeline 不可逆/不可约束求解，导致题目需要暴力；直接状态比对允许 Z3 求解，难度来自逆向+建模而非计算不可行性 |
| **ENC_EXPECTED_STATE 用 soKey 加密存储** | 选手必须先正确派生 soKey 才能得到目标状态；静态分析看不到明文目标；蜜罐污染 soKey 时 dump 出的目标也是错的 |
| **adjust_logging 首次调用缓存结果** | 避免 4 次重复文件 I/O，消除 Frida 中途注入导致的不一致状态 |

```
